import React from "react";
//import styles from './image_file_input.module.css'

const ImageFileInput = () => {
  const myStyle = {
    backgroundColor: "blue",
  };
  const image_file_input = (props) => <button>Image</button>;
};

export default ImageFileInput;
